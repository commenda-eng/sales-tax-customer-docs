---
title: "List supported registrations for a business entity"
openapi: "GET /public/companies/{companyId}/business-entities/{businessEntityId}/registrations/supported"
---

Returns the registration types available for a specific business entity within an affiliate-accessible company. The `businessEntityId` must belong to the provided `companyId` or the API returns `404 Not Found`.
