---
title: "Get a payment intent"
openapi: "GET /public/payment-intents/{paymentIntentId}"
---

Returns the payment intent id plus the linked `serviceRequestURL` in the app when the service request already exists.
